# twenty

Self-hosted [Twenty CRM](https://twenty.com) deployment for the Contabo VPS.

The deployment package (docker-compose, Caddy block, schema, MCP, backups, runbook)
lives in [`deploy/twenty/`](deploy/twenty/). Start with
[`deploy/twenty/README.md`](deploy/twenty/README.md).
